export default interface Theme {
    primary: string;
    background: string;
    card: string;
    disabled: string;

    font: string;
    fontSecondary: string;

    danger: string;
    warning: string;
    success: string;

    border: string;
}